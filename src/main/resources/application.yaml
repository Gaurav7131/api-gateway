spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: spac-core-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/v1/spacs/**
